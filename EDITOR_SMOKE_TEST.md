# Editor 冒烟测试用例（editor.html）

> 面向 `editor.html` 的**手动**核心冒烟测试，覆盖工具原有功能 + 近期优化改动。
> 目标：在每次改动 `editor.html` 后，用 ~15 分钟跑一遍核心路径，快速发现回归。
> 不追求穷尽，只覆盖关键路径与高风险点。

## 0. 前置准备

### 两种运行模式都要覆盖
- **本地模式（Local）**：直接用浏览器打开 `editor.html`，点 **Open** 选择工作目录。
  - 需要支持 File System Access API 的浏览器（Chrome / Edge）。
  - 工作目录结构：`<任务名>/pages.json`、`<任务名>/clean/*.png`、`<任务名>/translations/*.json`，可选 `story.txt`、`config.toml`。也支持 `work/<任务名>/...` 子目录布局。
- **服务端模式（Server）**：通过后端启动并带 `?t=<token>` 访问。
  - 后端需提供 `api/data`、`api/translations`、`api/image`、`api/save`、`api/config`、`api/story`、`api/story/save`、`api/page/delete`、`api/pipeline/run` 等接口。
  - 此模式下 **Open** 按钮与任务列表区隐藏，直接加载 token 对应任务。

### 测试数据
- 至少 1 个含**多页**、每页含**多个文字块**的任务。
- 至少 1 页存在**未翻译**文字块（用于进度/高亮校验）。
- 建议另有 1 个任务用于**合并**测试，1 个外部译文 JSON 用于**导入对比**测试。

### 标记说明
- 🆕 = 本轮优化新增/变更的行为（重点回归项）
- ♻️ = 状态集中化重构涉及、需重点验证无回归的路径
- 🌐 = 需在 **两种模式** 下分别验证

---

## 1. 启动与加载 🌐

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S1.1 | 本地模式打开 `editor.html` | 页面正常渲染；标题为 `Manga Translator Lite Editor`，无 `•` 前缀；右侧 Save 按钮禁用、无圆点 |
| S1.2 | 点 **Open** 选择工作目录 | 任务列表列出含 `pages.json` 的任务；选择失败时提示「Access denied」（红色 toast）|
| S1.3 | 服务端模式带 `?t=token` 打开 | 自动加载任务；**Open 按钮与任务区隐藏**；首页自动选中并渲染 |
| S1.4 🆕 | 本地模式观察侧栏 **Pipeline** 标签 | 标签置灰（`disabled`），鼠标悬停有「仅服务端可用」提示 |
| S1.5 🆕 | 观察 header 布局 | **UI 语言旗帜选择器在左侧 logo 旁**；右侧操作簇为 help/Open/译文语言/Reload/Import/Save；两个语言下拉不再并排 |

---

## 2. 任务与页面导航 ♻️

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S2.1 | 点击任务列表中的任务 | 加载该任务，页面列表刷新，首页选中并渲染 |
| S2.2 | 点击页面列表项 | 画布切换到该页，左侧分页计数同步，编辑面板刷新为该页文字块 |
| S2.3 ♻️ | 用底部 ← / → 翻页 | 当前页索引、页码输入框、首/末页按钮禁用态正确联动 |
| S2.4 ♻️ | 在页码输入框直接输入页号回车 | 跳转到对应页 |
| S2.5 🆕 | 任务名很长时观察列表 | 中间省略（保留头尾），悬停显示完整名 |

---

## 3. 画布渲染与缩放 ♻️

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S3.1 🌐 | 选中一页 | clean 底图正确显示；各文字块画出蓝色边框 + 序号标签；已翻译块叠加渲染译文（描边+填色，含竖排/横排）|
| S3.2 🆕 ♻️ | 点缩放按钮循环三档 | 依次 适应→宽度→100%；**按钮上短标签随之变化**（适应/宽度/100%）；画布尺寸相应变化 |
| S3.3 🆕 | 切换界面语言后再看缩放按钮 | 短标签与 tooltip 随语言更新 |
| S3.4 | 100%/宽度模式下用 ↑/↓（焦点在画布）| 画布容器上下滚动 |

---

## 4. 译文编辑与保存 ♻️

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S4.1 ♻️ | 在某块译文框输入文字 | 画布对应块约 300ms 后实时重绘译文；输入时 textarea **不丢失焦点/光标** |
| S4.2 🆕 | 输入后观察 Save 按钮与标题 | Save 按钮启用并出现**白色圆点**；浏览器标签标题出现 `•` 前缀 |
| S4.3 🆕 🌐 | 点 Save | 保存成功，**绿色** toast「已保存」；圆点与标题 `•` 消失；Save 重新禁用 |
| S4.4 ♻️ | 有未保存修改时切换任务/页面/语言 | 弹出「有未保存的修改」确认框；取消则不切换 |
| S4.5 ♻️ | 焦点在译文框，光标在首/末时按 ↑/↓ | 切换到上一个/下一个文字块并聚焦其译文框，画布对应块高亮 |
| S4.6 | 焦点在译文框按 Alt+←/→ | 即使在输入框中也能翻页 |
| S4.7 | 译文框内按 Alt/Ctrl+Z | 清空当前译文，提示「已清空译文」 |
| S4.8 🌐 | 切换目标语言下拉 | 加载该语言译文文件，画布与编辑面板刷新；进度角标整体刷新 |
| S4.9 🆕 | 观察编辑面板中的文字块卡片 | 卡片正文为**左右两列**：左「原文」（正常字号、可选中、超长可滚动）/ 右「译文」（输入框），上方各有小标签 |
| S4.10 🆕 | 找一个**超长原文**的块 | 原文列出现内部滚动条（不撑爆卡片），便于与右侧译文逐句对照 |
| S4.11 🆕 | 拖动编辑面板与画布之间的分隔条 | 面板宽度可调，两列随之伸缩；宽度记忆到下次打开（localStorage）|

---

## 5. 翻译进度可视化 🆕 ♻️

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S5.1 | 观察页面列表 | 每页显示 `已译/总数` 角标：全译完=**绿色**，部分=**琥珀色**，无文字页=不显示 |
| S5.2 | 观察画布上的文字块序号标签 | 未翻译块标签为**琥珀色**，已翻译为蓝色 |
| S5.3 | 观察编辑面板卡片 | 未翻译卡片标题前有**琥珀小圆点** |
| S5.4 | 给一个空译文块输入文字 | 当前页角标分子 +1（如 2/5→3/5）；画布该块标签转蓝；约 300ms 后渲染译文 |
| S5.5 | 清空一个已译块 | 角标分子 -1；该块标签转琥珀 |
| S5.6 | 切换目标语言 | 所有页角标按新语言重算，且当前页仍保持高亮选中 |

---

## 6. 页面缩略图 🆕 🌐

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S6.1 | 打开多页任务，观察页面列表 | 每项左侧显示 clean 图缩略图（顶部对齐裁剪）|
| S6.2 | 慢速滚动页面列表 | 缩略图**懒加载**：滚入视口才请求（DevTools Network 可见仅可视项加载）|
| S6.3 | 本地模式滚动大量页面 | 不出现明显内存增长（blob URL 在图片解码后即回收）|
| S6.4 | 服务端模式点开某页 | 缩略图与主图共用 `api/image` URL，主图加载走缓存、不重复下载 |
| S6.5 | 切换到另一任务 | 旧缩略图不残留，新任务缩略图正常加载 |

---

## 7. 区域编辑（文字块边界）♻️

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S7.1 🆕 | 点编辑区域按钮（铅笔）进入编辑模式 | 按钮高亮；画布顶部出现**常驻蓝色提示条**（拖手柄缩放/拖框移动/空白新建）；光标变十字 |
| S7.2 | 选中块后拖动 8 个手柄之一 | 实时缩放该块边框；松开后几何写入 pages.json（自动保存）|
| S7.3 | 拖动块体 | 整块移动；松开后保存；卡片中 `宽×高` 更新 |
| S7.4 | 空白处拖拽出一个矩形 | 新建用户文字块（带 ✎ 标记），自动选中；进度角标分母 +1（新块未译→琥珀）|
| S7.5 | 退出编辑模式 | 按钮取消高亮；顶部提示条消失；光标恢复 |
| S7.6 | 非编辑模式下点击画布中某块 | 选中该块并在编辑面板高亮对应卡片 |

---

## 8. 渲染设置浮层 🆕

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S8.1 | 点底部齿轮（滑块）按钮 | 弹出「渲染设置」浮层，含 文本框缩放 / 最小字号 / 扩框上限，均为带文字标签的输入框 |
| S8.2 | 修改文本框缩放值 | 写入 pages.json `box_scale`；画布按比例缩放文字框并重绘 |
| S8.3 | 修改最小字号 / 扩框上限 | 写入对应 per-task 字段；留空则回退 config.toml（placeholder 显示继承值）|
| S8.4 | 点浮层外部区域 | 浮层关闭，齿轮取消高亮 |
| S8.5 | 切换界面语言 | 浮层标题与标签随语言更新 |
| S8.6 | 窄屏/拖窄窗口 | 底部工具栏不溢出（渲染参数已收进浮层）|

---

## 9. 任务合并（本地模式）

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S9.1 | 点 Merge 进入合并模式 | 任务项出现勾选框；出现「确认合并/取消」按钮 |
| S9.2 | 按顺序勾选 ≥2 个任务 | 勾选顺序显示序号徽章（决定合并顺序）|
| S9.3 | 少于 2 个时点确认 | 红色提示「请至少选择两个任务」|
| S9.4 | 确认合并 | 生成 `merged_<时间戳>` 任务：页面按 `0001.ext` 顺序重命名、块 id 全局重编、各语言译文按新 id 正确迁移、clean 图复制；成功 toast |
| S9.5 | 打开合并结果任务 | 页面顺序、译文与原任务对应一致（无串行/错位）|

---

## 10. 导入译文对比 🆕

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S10.1 | 点导入按钮选一个译文 JSON | 与当前译文逐块比对，弹出差异列表（块 id·页码·原文片段·当前→导入）|
| S10.2 | 文件名语言与当前编辑语言不一致 | 先弹确认框提示语言不匹配 |
| S10.3 | 无差异时导入 | 提示「没有差异」，不弹列表 |
| S10.4 | 勾选部分差异并应用 | 仅应用所选，保存，画布/面板/进度角标刷新；绿色 toast「已应用 N 处」|
| S10.5 | 导入非法/格式错文件 | 红色提示「无效的译文文件」|

---

## 10A. 跨页搜索 / 替换 🆕 🌐

> 编辑面板标题栏的放大镜按钮触发；纯前端操作内存中译文，保存走现有接口，两种模式一致。

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S10A.1 | 点编辑面板标题栏放大镜按钮 | 弹出「搜索/替换」模态，含查找框、替换框、「同时搜索原文」勾选；自动聚焦查找框 |
| S10A.2 | 在查找框输入一个出现在多页译文中的词 | **实时**列出全任务命中项（`P页 · #块` + 片段），底部显示命中数 |
| S10A.3 | 勾选「同时搜索原文」 | 列表加入仅原文命中的块（标记「原文」），用于定位 |
| S10A.4 | 点某条命中项 | 模态关闭并**跳转到对应页+块**（按 block id 定位，正确高亮，即使该页首次进入会重排）|
| S10A.5 | 填替换词后点「全部替换」 | 所有命中项的**译文**被替换（原文不动）；保存；画布/面板/进度刷新；绿色 toast「已替换 N 处」|
| S10A.6 | 取消部分勾选后点「替换所选」 | 仅替换勾选项 |
| S10A.7 | 替换框留空执行替换 | 等于删除该词 |
| S10A.8 | 查找词为空 / 无命中时替换 | 无副作用；无命中显示「无匹配」 |
| S10A.9 | 未选任务时点搜索按钮 | 红色提示「选择一个页面开始」，不弹模态 |
| S10A.10 | Esc 或点遮罩 | 关闭模态 |

---

## 11. 故事剧本 story.txt 🌐

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S11.1 | 切到 Story 标签 | 加载任务的 story.txt 内容；未选任务时禁用输入与按钮 |
| S11.2 🆕 | 加载长文本 / 持续输入 | 文本框**高度随内容自适应**（最小约 120px），长剧本不挤在小框里 |
| S11.3 | 选择一个模板 | 套用对应语言模板文本（已有内容时先确认覆盖）；套用后高度自适应 |
| S11.4 🌐 | 点保存 | 本地写入 story.txt / 服务端 POST `api/story/save`；成功 toast（服务端失败时回退为下载）|

---

## 12. Pipeline（仅服务端）🆕

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S12.1 | 服务端模式切到 Pipeline 标签 | 正常进入；命令下拉切换时输入/输出/起始索引等字段按需显隐 |
| S12.2 | 运行某命令 | 终端面板打开并流式输出日志；状态/错误行着色；完成提示「Pipeline completed」并按需重载数据 |
| S12.3 | **本地模式**尝试进入 Pipeline | 被拦截，红色提示「仅服务端可用」，不切换标签 |

---

## 13. 页面删除 / 页面信息 🌐

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S13.1 | 点页面项 ℹ️ 信息按钮 | 弹出页面信息（页码/任务/原始文件/clean 路径/完整路径/尺寸/块数等）；Esc 或点遮罩关闭 |
| S13.2 | 点页面项 🗑 删除 | **二次确认**后删除：移除该页、其块译文（全语言）、clean 图；进度与列表刷新；选中态修正到相邻页 |
| S13.3 | 删除最后一页 | 列表为空时回到初始空状态 |

---

## 14. 多语言 / 快捷键 / 帮助 / 无障碍 🆕

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S14.1 | 切换界面语言（🇬🇧/🇯🇵/🇨🇳）| 全界面文案、tooltip 即时切换；刷新后保持（localStorage）|
| S14.2 | 点 `?` 帮助按钮 或 按 `?` 键 | 弹出快捷键帮助浮层；列出 ←/→、↑/↓、Alt+←/→、Alt/Ctrl+Z、? ；Esc/点遮罩关闭；重复触发不堆叠 |
| S14.3 | 焦点在任务列表时按 ↑/↓ | 在任务间上下切换 |
| S14.4 | 用读屏或检查元素查看图标按钮 | 各图标按钮含 `aria-label`（随语言）；图标为统一 SVG（无 emoji）|

---

## 15. 安全性 / 注入回归 🆕

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S15.1 | 在某条译文中输入 `</textarea><b>x</b>` 等标记 | 仅作为**纯文本**显示，不破坏布局、不执行；保存后重载仍为纯文本 |
| S15.2 | 原文（OCR）含 `<`、反引号等特殊字符的块 | 编辑面板原文区按纯文本正确显示，不破坏卡片结构 |

---

## 16. 状态集中化综合回归 ♻️（重点）

> 本会话将 `currentPageIndex / selectedBlockIndex / isDirty / zoomMode / needsSort / editMode / activeFocusArea` 收敛进 `state` 对象，并引入 `render()` 调度。以下串联用例确保无回归：

| ID | 步骤 | 预期结果 |
|----|------|----------|
| S16.1 | 连续翻页 + 选块 + 编辑 + 保存 + 切语言 一气呵成 | 全程画布、页面列表、编辑面板、进度角标、未保存指示均同步正确，无错位/卡死/控制台报错 |
| S16.2 | 进入编辑模式新建块→编辑译文→退出→翻页→返回 | 新块、译文、几何、进度均持久且正确显示 |
| S16.3 | 方向键在 任务/页面/编辑器/画布 四个焦点区间切换导航 | `activeFocusArea` 行为符合预期，各区导航互不串扰 |
| S16.4 | 全程打开浏览器控制台 | 无 `state is not defined`、无 `render` 递归爆栈、无未捕获异常 |

---

## 附：快速冒烟（~5 分钟最小集）
S1.1 → S2.1 → S2.2 → S4.1 → S4.2 → S4.3 → S4.9 → S5.4 → S6.1 → S7.1 → S8.1 → S10A.2 → S10A.5 → S14.2 → S15.1 → S16.1
